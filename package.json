{
  "name": "iso",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "pm2 start index.js --time --name iso-scapper",
    "api":"pm2 start memory.js --time --name iso-memory"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.17.0",
    "node-cron": "^3.0.2",
    "puppeteer-core": "^18.0.5",
    "request": "^2.88.2"
  }
}
